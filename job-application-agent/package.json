{
  "name": "decentralized-job-agent",
  "version": "1.0.0",
  "description": "Privacy-first decentralized AI job application agent with multi-language support",
  "main": "backend/server.js",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm start",
    "build": "cd frontend && npm run build",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install && cd ../ai-engine && npm install && cd ../blockchain && npm install",
    "start": "cd backend && npm start",
    "test": "npm run test:backend && npm run test:frontend",
    "test:backend": "cd backend && npm test",
    "test:frontend": "cd frontend && npm test"
  },
  "keywords": [
    "decentralized",
    "privacy",
    "gdpr",
    "ai",
    "job-application",
    "blockchain",
    "multi-language"
  ],
  "author": "Decentralized Job Agent Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}